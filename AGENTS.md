This is the main app repo in a multi-repo IntelliJ workspace.

- `CinderStride` is the primary application.
- `Dirigo` in `../Dirigo` is a separate dependency repo.
- Tasks in this workspace may require reading or changing code in both repos.
- Do not treat `Dirigo` as the primary project just because another session started there.
